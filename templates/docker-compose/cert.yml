  web:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./rootfs/etc/Caddyfile:/etc/Caddyfile:ro
      - ./rootfs/etc/certs:/etc/certs:ro
    env_file: .env
    environment:
      ACME_AGREE: "true"
